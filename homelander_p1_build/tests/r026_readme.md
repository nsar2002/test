# R026 — exact-game third-party Lua-executor compatibility audit (SOURCE ONLY)

**Observed game SHA256:** `ed0870eb24ace5565a0245ade42114f0724b881d3d75b793f376e9bb184f8d51` (`prototypeenginef.dll`, 20,210,960 B). **Observed original loader SHA256:** `bc48bab2cf07eaf616e23ba9eef1922ea5e8590f722b6657a8f2ed986fe6f20e` (`binkw32.dll`, Ultimate ASI Loader). The first real R007/v003 BOOT/F4/F9 user evidence has three F4 attempts with PLAYER=nil and all original named GOH finders nil, and two original F9 aborts without player; absolutely NO official v003 PASS.

Read-only analysis of external `uranami/Prototype1-Lua-Tool` source **PINNED** to public commit `c54b297105b54dec992885e81b083c4c0b95f7d3`, blobs `src/sdk/interfaces.cpp` = `3d334b20f20842cf63f1fa964d76c3cf13b40730` and `src/hooks.cpp` = `04c5aef179a2bc82e5c78d3a34d98de398ad8323`. It contains a real Prototype 1 Lua executor with one-shot script editor and Lua loader, but its `src/sdk/lua_globals.hpp` explicitly states its curated global list came from **prototype2engine.dll**; ignore that list as an authority for the user's actual Prototype 1 engine. The 11 upstream Lua API signatures, DoLuaBuffer, LuaScriptManager global and GOMUpdate pattern were each independently found EXACTLY ONCE in the user's matching original DLL using source-derived wildcard byte scans (14/14). This is an **offline pattern match, not a successful injection, tested Lua ABI, verified Lua global player or runtime safety**.

**Critical coexistence risk:** our original R018/native `homelander_p1.cpp` and the external executor both hook the EXACT SAME GOMUpdate game routine (pattern `53 55 56 57 E8 ? ? ? ? E8 ? ? ? ? E8`). Ours installs a 5-byte JMP/trampoline; external uses SafetyHook. External also hooks D3D EndScene/Reset, DirectInput mouse/keyboard, WndProc. Multiple ASI plugins are supported by the installed Ultimate ASI Loader, but whether these particular native patch chains and key/window hooks coexist safely is UNPROVEN. Do NOT add the executor to `G:\prototype1\Prototype`, do NOT overwrite the frozen original `homelander_p1.asi`, original R007 manager/manifest or first-gate logs, and do NOT register PASS or install v004+.

**Safest optional live research route for Gemini/Antigravity later, only after explicit user's approval:** create a disposable full **separate** game directory with genuinely independent executable and game assets; never use a junction/link to the active game root and do not reuse its `.homelander_promotion` state or receipts. Back up shared game saves/config separately because a clone may still use the same per-user save directory. In the disposable copy run ONLY ONE ASI: the separately inspected source-pinned Lua executor, **without** our `homelander_p1.asi`; retain the original unchanged Ultimate ASI Loader and Bink replacement. Validate the game's running `prototypef.exe` executable path resolves to the disposable copy, not the active original. No automatic looping Lua scripts. In actual controllable Alex gameplay, use the executor's one-shot editor to run bounded **R024 key/type census** first, capture only key names/types; if this does not identify a game-provided player alias, consider one-shot **R025 camera-nearest observer** only after explicitly accepting that even a pcall cannot contain a native access violation/crash. Record its exact `candidate type`, `online player index`, nil/error statuses but no GOH addresses/userdata or gameplay actions; distinguish nearby NPC from local Alex. Export the diagnostic text OUTSIDE the original game directory; stop the experimental game completely. Any candidate remains unverified for the original R007 v003 game until a separately reviewed fix and a fresh authentic F4+F9 test produce CAMERA/HIT and the ORIGINAL manager Check verifies the real new v003 gate.

The audit script `r026_exact_executor_pattern_and_dualhook_audit.py` requires five EXISTING explicit input paths: `--engine` (exact user game DLL), `--loader` (exact original Bink Ultimate loader), `--upstream-interfaces` and `--upstream-hooks` (above pinned upstream source files), and `--original-native` (the frozen homelander_p1.cpp). It hashes both user's game binary and loader, extracts the patterns from the actual upstream source and fails closed on duplicate/missing pattern or source divergence. It READS bytes only and writes JSON to stdout, never loads DLLs or touches the game. GitHub CI uses independently pinned upstream Git blobs and synthetic binary tests ONLY; NO proprietary game executable or user evidence is uploaded to GitHub. Current real user R007/v003 remains active, untouched and NOT PASSED.
