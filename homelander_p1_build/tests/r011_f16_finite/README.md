# R011 — F16 finite ray and finite lifetime guards (DORMANT SOURCE ONLY)

The prior actual staged dynamic-aim F16 code used Lua range = tonumber(value) or 500 then two < and > clamps, but NaN bypasses both comparisons and produces a NaN rayEnd passed into fre_LineOfSightTest every active render tick. Independently, NaN user-configured max render-frames/max ticks bypasses both timeout clamps and timeout comparisons. These are source-level hazards, not observed Prototype runtime results. This branch reproduces both cases with real previous source and deterministic Lua engine mocks.

The isolated lasersight_dynamic_aim_gate_v010_finite_DORMANT.lua rejects nonfinite range BEFORE fre_LineOfSightTest and rejects either nonfinite lifetime limit at the BEGINNING of the tick before any native camera/LOS/laser callback. The renderer disables dynamic mode and returns false; valid finite settings retain the previous clamping and normal positive tick. No original F16 staged file was edited. The 22-file native-path source integration assembler now makes FIVE atomic source overrides: R010 four F9/F10/F11/F12 guarded variants plus new F16 finite variant at native original STAGED basename. Other 17 native Lua runtime sources and four original v003 baseline files are still byte-identical.

Tests reproduce old NaN ray reaching a mocked native LOS, old invalid limits entering native tick, new NaN range/limit rejection with ZERO native LOS/laser submissions and positive finite dynamic render mock. Full independent Lua5.1 syntax and 22-module native definition-load order, R009/R010 F9-F12 reentrancy suites, scratch Win32 unchanged C++ compilation and source-only artifact integrity are included. No ASI, installer, packaged game stage, original R007 release/manager/manifest or user game files modified. This source bundle is NOT installable and not actual in-game validation. Continue with original R007 real v003 BOOT/F4/F9, close game and Check before any later staged release.
