# R034 — Exact original v003 + R031 + R033 reachable GOH probe (SOURCE ONLY)

The genuine R033 clone had original `runtime_probe.lua` followed by R031 and a R033 GOH observer appended after R031's camera-OFF `return`. Original F4 and the R031 census ran, but R033 was unreachable; the R033 exporter correctly refused to invent observations. R034 retains the exact original R007/v003, exact R031 source and exact unchanged R033 five-GOH observer, and moves the observer INSIDE the camera-OFF branch, AFTER the R031 `END` diagnostic message but BEFORE its top-level `return`. It does NOT switch on the camera observer, call native physics setters, set `PLAYER` or `HOMELANDER_PLAYER`, or grant original manager PASS.

Exact previous clone R033 probe SHA256 `19242ddb0f860efefe63dc36c416881acd9055d0d7b38e14a53aafbc015444d1`. Reassembled fixed clone-only probe SHA256 `40ca8cb1ce905543e6dc73b14ef2d57e9b1ac7f7ca55f88eb88232dead6bec72`. Exact R033 observer source SHA256 `f973511947d2034ef986a533a6737a294589bc8ebcfbaebb02fadbd9388ba539`; R031 combined original SHA256 `d387fceddb45741aef7974c6b66b323f11fec49aff3ebdfb7cb08025edbf4d79`. The standalone repair patcher must reassemble and verify the exact resulting R034 SHA before touching any clone file, back up previous original R033 probe and full prior research log externally, and never touch original active game root or frozen R007 release.

The real Lua5.1 CI compiles the **full exact combined source** (not just the observer) and executes it at BOOT (five-candidate native observer skips), then F4 and repeat F4 (five engine custom-type GOH aliases inspected incl. two negative controls), with no player assignment or gameplay mutation; separate Python tests use NONPLAYABLE local synthetic tiny game roots, genuine pinned old source and earlier authentic R031 log bytes, and synthetic post-R034 observations only. Offline CI PASS is NOT genuine R034 gameplay evidence and never authorizes v004 promotion. The only next gameplay should be a single F4 in the already isolated same cloned game after R034 local Stage and Verify succeed. The R034 exporter returns a research-only complete/partial log ZIP even if observations are truncated; it never turns missing observations or log text into a game-gate PASS.
