# Homelander P1 R008 — native-path 22-Lua integration assembly (DORMANT SOURCE CANDIDATE)

Goal: move beyond four disconnected mock scripts and establish a complete atomic 22-file Lua payload consistent with EVERY ExecuteLuaFile(...) path in the original P1 native C++ bootstrap. The native C++ file is unchanged, so existing F9/F10/F11/F12 keyboard dispatch resolves the four new guarded implementations when the candidate payload is eventually approved for a separately gated release.

Assembler tests/r008_22lua_integration/assemble_source_only.py copies EXACTLY 22 unique native-file paths (24 load occurrences): four guarded variants are mapped ATOMICALLY to the original four STAGED basenames, four early v003 baseline scripts remain byte-identical from v003_transfer, and all remaining 14 native-expected Lua files copy unmodified from the repository. Output contains complete lua_p1/, immutable file hashes and a provenance manifest, but NO ASI, EXE, DLL, stage installer, executable game patch, or release promotion manager. The original 22 source scripts, all original v003-v016 packages and official R007 manager/manifest remain untouched.

CI separately cross-compiles the UNCHANGED native C++ in a scratch folder using MinGW-w64 Win32 solely to catch native build issues and inspect PE32 imports. That scratch ASI is NOT shipped in the candidate download and is NOT authorized for installation. CI independently parses ALL 22 staged Lua files using Lua 5.1, executes the previous actual-source F9-F12 numeric/provenance regression, checks source manifest and original baseline bytes, and verifies no excluded binary or mutation file is present.

Important boundary: compiling a 32-bit Windows ASI and making all 22 native file paths exist does NOT establish that the file can load in a user's Prototype build, that player GOH/camera offsets exist at runtime, that two real eye beams render, or that held flight/damage are safe. This candidate is a future post-v003-gate integration research result, not the active game's v003/v004 promotion path. DO NOT copy this payload into the existing R007 staged ZIPs or the user's game root without a new versioned/reversible release and actual sequential human game evidence.

First mandatory real-world verification is still original R007 stage v003 BOOT → F4 → F9, close game, inspect authentic v003 logs and check in original manager. If F9 is negative or game crashes, do not promote.
