# R009 — F11 eye-measurement epoch + F12 dual provenance, DORMANT

## Reproduced fault
The previous fail-closed F11 only verified current HOMELANDER_PLAYER identity. A synchronous same-player nested F11 call during go_GetJointPosition could publish a newer eye point, then the older outer F11 could overwrite the eye proof with its obsolete point. The previous F12 verified F9/F10 attempt generations but not the identity of the F11 measurement after native callbacks. A newer F11 for the same player during the first laser call could therefore leave F12 free to submit the second call against a changed eye proof.

## Isolated atomic source correction
New eye_origin_probe_v006_epoch_DORMANT.lua increments a monotonic HOMELANDER_EYE_ORIGIN_EPOCH on every F11 attempt and clears the old proof/previous F12 receipt. It publishes HOMELANDER_EYE_ORIGIN_VERIFIED_EPOCH only after getter, optional player getter/log, and final valid/player check, and refuses to overwrite a newer nested F11 proof even for the same player. New dual_eye_freeaim_render_v007_f11_epoch_DORMANT.lua snapshots the exact verified F11 generation and requires it to remain identical before first laser call, after first, and after second, on top of existing F9/F10 proven generation and player/target bindings. Partial native effects from the first call remain irreversible if an engine callback fails; the code only prevents a stale SECOND call and a false success receipt.

The R008 assembler is updated ATOMICALLY: exactly four native F9/F10/F11/F12 staged filenames still receive their paired guarded variants, with new F11 and F12 generated together. All other 18 Lua filenames, four original v003 baseline source files, native C++, original R007 manager/manifest and staged archives remain unchanged.

## Evidence and boundaries
Actual-source deterministic Lua5.1 regression first reproduces prior F11 stale x1 overwriting newer x2 in same-player nested callback, then confirms newer F11 survives and older returns false. Another mock nests a later successful F11 inside the first ai_Laser call and verifies no second ai_Laser invocation and no false dual-eye PASS. Positive F9 -> F10 -> F11 -> F12 still submits exactly two mocked eyes. A failed later F11 invalidates prior eye/laser receipts. The complete 22-file native-path Lua source assembly and read-only definition load-order smoke are re-run. This is NOT actual Prototype gameplay evidence, no real rendering/flight/damage tested. No installable ASI/EXE is distributed. Do NOT overwrite original R007, stage ZIPs, or user game. The authentic first-live gate remains original R007 v003 BOOT/F4/F9; close game; manager Check before any later promotion.
