# R008 ordered v003 first-live-gate manager — SOURCE-ONLY candidate, NOT R007 release

The original R007 manager's Check path independently uses regular-expression presence across the whole runtime log; that by itself does not establish that READY, F4 and F9 CAMERA/HIT occurred in order within one fresh test. Example synthetic log READY -> F9 -> CAMERA -> HIT -> F4 has all mandatory regexes but reverses the F4/F9 dependency.

The isolated candidate P1_PROMOTION_MANAGER_ORDERED_CANDIDATE.ps1 is a copy of the frozen manager with an additional Check-only temporal check for stage v003: most recent READY of the stage build -> F4 -> most recent F9 dispatch -> CAMERA -> a nonnil target HIT. Stale matches from before a newer READY or a later F9 MISS are rejected. All original fatal-log checks, loader/ASI hashes, active game-process guard, receipt format and per-stage dependencies are otherwise unchanged. Check continues to require actual game shutdown. The candidate only improves temporal log consistency: it does not cryptographically authenticate a log, guarantee screen visuals or prove that runtime player state was valid; user-generated fixture logs in CI are explicitly synthetic.

The fixture P1_R008_ORDERED_GATE_SYNTHETIC_FIXTURE.ps1 uses throwaway files under Windows RUNNER_TEMP, a non-executable fake prototypef.exe, and fake loader/ASI; it never launches Prototype, never reads or writes the user's game root, and never invokes a real game process. It invokes the ORIGINAL R007 manager and the candidate against the same synthetic unordered log, asserts original's overly permissive pass, clears synthetic state, asserts candidate rejection of out-of-order, old READY, later F9 MISS and HIT-before-CAMERA evidence, then validates ordered positive evidence and exactly one synthetic PASS receipt. This is a demonstrated mock fixture, not real-game result.

No original R007 package, original manager, original promotion manifest, v003-v016 stage ZIP, game files, native ASI, or Lua runtime bridge has been replaced. The candidate is NOT an authorized drop-in replacement for the frozen R007 first-live runner until the actual v003 run has been completed and a new separately reviewed, hash-locked release is packaged. Current mandatory first real-world action remains ORIGINAL R007 stage v003 BOOT/F4/F9, close game, then Check with authentic evidence; do not promote beyond v003 on synthetic CI or source-only guard results.
