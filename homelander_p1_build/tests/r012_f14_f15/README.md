# R012 — F14 exact proof and F15 finite held lifetime, DORMANT source-only

The original staged F14 one-shot LaserSight handler never cleared HOMELANDER_LASERSIGHT_ONESHOT_LAST_OK/LAST_TARGET on a later failed F14 attempt. A previous success for NPC could still satisfy the original F15 held LaserSight gate even after a newer F14 failure and a new F9/F10 attempt on the same NPC. F15 also failed to reject a NaN HOMELANDER_LASERSIGHT_MAX_HELD_FRAMES; Lua NaN bypassed its <, > and >= comparisons, so held mode could continue beyond the intended timeout. The actual prior source behavior is reproduced under deterministic Lua5.1 game mocks; no real game run was performed.

A new isolated F14 native-source guard clears its prior one-shot success, target and F9/F10/F11 provenance at the START of each attempt. It captures the exact current R010 F9/F10 and R009 F11 generations and a separate F14 attempt counter, rechecks the provenance after target/eye getters before native LaserSight submission and again after the native callback before committing a success receipt. A newer F14 attempt or changed target/eye invalidates the older outer callback. Native visual effects, once submitted, cannot be undone even if the post-submit check fails.

A new paired F15 held-source guard requires the most recently successful F14 receipt to match the exact active F9/F10/F11 provenance, not merely the same player or target name/GOH. It locks the exact F14 attempt at toggle activation and rechecks the gate each tick, refusing stale same-target refreshed F9/F10 or a different F14 attempt. It validates max held frames as a finite number at the start of EVERY tick and disables held rendering BEFORE any game getter/native LaserSight call on NaN. On invalid lifetime it also clears Lua held-stability success to prevent F16 reusing that proof; routine F15 -> F16 handoff continues to preserve previous native/Lua F15 stability behavior. Ordinary finite limits retain the previous min/max clamps and automatic timeout.

The complete 22-native-script path source assembler now atomically maps SEVEN protected source modules (F9/F10/F11/F12, F14/F15/F16), retains the other 15 native paths unmodified and preserves four original v003 Lua baselines byte-for-byte. Lua5.1 regression proves original stale F14/F15 and NaN F15 cases, new failed latest F14 clears success, new F15 refuses same-target stale F9/F10, new NaN limit never submits native laser and finite 60-frame mock held mode times out. Full R009/R010/R011 regressions, actual native definition-load smoke and unchanged Win32 C++ scratch compile run in CI. No ASI/installer or live game files are included or changed; original R007 and stage ZIPs stay frozen. Real on-PC game v003 BOOT/F4/F9 then shutdown and Check is still the mandatory first genuine evidence gate.
