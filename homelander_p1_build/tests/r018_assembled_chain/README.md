# R018 — one complete native-order assembled F9-F21 source payload dispatch smoke (NO GAME)

Previous R008-R017 CI loaded the assembled 22 native-path Lua definitions and ran each actual-source old/new safety regression, but each regression loaded its own source variants, so a missing atomic dependency or cross-module mismatch inside the assembled artifact could escape the isolated tests. This R018 exercise is a distinct integration check of the EXACT output from assemble_source_only.py, not individual repository scripts.

The Lua5.1 test tests/r018_assembled_f9_f21_native_order.lua creates an isolated deterministic fake game engine, defines strict Vector/GOH/camera/LOS/joint/transform/health/damage/VFX API mocks, loads each of the 22 assembled lua_p1 files in their original native C++ definition order, and asserts zero automatic mutation. It dispatches actual staged native function names F9/F10/F11/F12/F13/F14/F15 (120 successful held mock frames)/F16 (120 successful dynamic mock frames)/F17 (exactly one mock damage)/F18 (exactly one mock VFX)/F19 (15 valid continuity samples)/F20 (30+ diagnostic dry-run frames)/F21 (read-only actual hit-direction payload) in a SINGLE Lua state. It also checks that player B cannot borrow the completed player A F17-F20 chain and that F21 does not cause extra mutations.

The fake native HL_LaserSightSubmitDual distinguishes one-shot, held and dynamic modes, requires one-shot before held and 120 mock held submissions before dynamic, and exposes mock native held/dynamic stability queries only after 120 actual submits each. This verifies internal counter/ordering agreements only under those deliberate mocks. The F4 originating player is explicitly simulated by setting HOMELANDER_PLAYER=A; this does NOT test F4 real engine player discovery, true rendering, ABI, real GOH lifetime, camera selection, engine timer, physical flight, visual laser or damage/VFX behavior. Real first gate remains ORIGINAL R007 v003 BOOT/F4/F9 on user's Windows Prototype, then close game/Check; no original manager/staged ZIP/ASI/active game installation may be changed based on this mock. Source-only artifact remains non-installable.
