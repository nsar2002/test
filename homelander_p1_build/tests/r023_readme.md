# R023 — audit only the EXACT observed user's Prototype engine DLL

This isolated companion is not an installer or player acquisition fix. It pins the 20,210,960-byte SHA256 ed0870eb24ace5565a0245ade42114f0724b881d3d75b793f376e9bb184f8d51 original engine DLL identified by the authentic v003 evidence. On the user's computer, Python 3 can run the separate static audit without launching the game or loading the DLL: `python r023_exact_engine_player_source_audit.py --engine "G:\\prototype1\\Prototype\\prototypeenginef.dll"`. The script emits bounded JSON to standard output and writes nothing; it refuses binaries with mismatched SHA256, PE type or section bounds. An optional redirect may write results OUTSIDE the game directory, but is not required. The CI tests only synthetic fake PE bytes and NEVER uploads, stores or distributes the user's proprietary engine DLL or evidence log.

**Exact observations:** In the real v003 game's loaded DLL, exact NUL-terminated registration markers include go_FindGOHByName, go_FindTransientGOHByName, go_FindGOHByUID, online_GetNumPlayers, online_DeterminePlayerIndex, cm_SetCameraAvatar and PlutoConvertIndexToGOH. Third-party Lua tool catalog includes go_IsPlayer and go_GetGOHByName, but those exact NUL-terminated markers are absent from THIS exact game binary; do not infer those functions can be called in this runtime, nor infer internal implementation is absent just because its string was not found. A positive string or PE .text pointer reference alone also cannot prove callable Lua semantics. The string MainCharacter is not an independently verified GOH name; no new blind name guesses are installed. cm_SetCameraAvatar is a setter, and the parameter shape or engine caller must be established before provenance use. Never invoke this setter as a read-only getter. PlutoConvertIndexToGOH exposes an index conversion name but no verified player index-to-GOH mapping is established; do not guess index=0 or pass arbitrary pointers.

**Source of known failure:** The authentic v003 BOOT/F4/F9 log shows PLAYER global nil, seven named candidates nil via both functions, no verified player, two F9 ABORTs, zero CAMERA/HIT, no manager PASS. The R018–R021 complete Lua simulations supply a fictional mock player A, and do not override that genuine negative. The original R007 manager/manifest/v003 game files remain unchanged. The next useful discovery is identifying the actual game's player GOH provenance (game camera avatar assignment or another source-backed handle), validating it by independent engine-native player evidence, and only then building a SEPARATE source-only candidate plus authentic game test; no v004+ promotion until v003 genuinely passes.
