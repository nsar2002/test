# R033 — Observational exact-engine custom GOH global provenance (CLONE ONLY)

**Real first R031 ZIP inspected**: exact SHA256 f533cc16b9a3b57525b0486fdc115ba135ac8535cc4b339543adf84af11b1da6, internal log SHA256 40d23232ada3eff0127d179d5564ea5d8284e8c2513cdfc55dfb54464bee2bee, 292 log lines, one original F4 (line 185), followed by complete census (lines 222–288). At both startup and F4, `MainCharacter`, `PLAYER_1`, `playerCharacterID`, `gameFlowMachineGOH` and `mainMissionGOH` are all Lua **engine-custom** type `GOH`, NOT normal Lua `userdata`. Original `PLAYER` is nil, all original 7 name guesses nil, so original v003 F4 still has NO verified player, F9 not run and official firstgate NO PASS. Full census matches 107, emits all five GOH names (other count 6, including numeric E_ICON_ALEX); priority 64 does not hide any extra GOH. Relevant goal: distinguish three player-named globals from two negative controls by genuine engine-native read-only evidence, not from names/type alone.

The separate **source-only** file `r033_goh_global_provenance_clone_only.lua` MUST NEVER be installed into the user's official `G:\prototype1\Prototype`. It may be APPENDED only in the separately verified original R031 clone `G:\prototype1\Prototype_R031_NATIVE_DIAGNOSTIC\lua_p1\runtime_probe.lua`, preserving exact previously verified R031 bytes, and re-hashed/pinned by a separate offline staging tool. The same ORIGINAL v003 ASI runs it from Lua bootstrap and at F4; to avoid native candidate calls during bootstrap, the observer skips unless the actual pinned flight-controller function `Homelander_FlightNativeTick` is available. This tests the original loader order but is not unforgeable proof of a physical F4 key event. Game must be CLOSED before any future clone-only stage or log export; keep original game, original official manager/state and original R007 release immutable. No external Lua executor, no second ASI and no F5–F21/F9 gameplay controls.

On an isolated subsequent game test, the observer reads only the five **already seen** `_G` globals; it checks exact custom `type(h)=="GOH"`, then `go_IsValid` **must return true** before any further native calls. It invokes read-only `go_GetPosition`, `ai_GetPhysicsVelocity`, `online_DeterminePlayerIndex` on a valid GOH with position+velocity, and optionally `go_GetDebugName`, logging just availability, bounded integer player index or sanitized short debug name. The two mission/flow GOHs are NEGATIVE CONTROLS; a control unexpectedly returning index zero means player index alone is non-discriminating. Never tostring(handle), log address, dump raw GOH/Vector or assign `HOMELANDER_PLAYER`, `PLAYER`, official manager receipts or setter gates. Strict post-analysis must NOT declare local Alex identity from an index alone; retain an observation requiring independent controllable-player behavioral correlation and a fresh legit original R007/v003 gate after separately reviewed fixes. Lua pcall catches Lua errors but NOT native access violations: any real engine-native call could still crash the isolated test clone; backup saves/settings and use only the independent clone. The current file is SOURCE ONLY, NOT installable standalone and NO actual R033 live result exists.

Source-only Lua5.1 tests use custom type() returning GOH for opaque newproxy objects, non-stringifiable values, negative control, absent player and strict boot skip; this cannot substitute for game results. Prior R031 official live gate remains unpassed.
