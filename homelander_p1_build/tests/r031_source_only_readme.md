# R031 — Deterministic candidate-first global census (source-only)
The actual user's original R007/v003 first-game evidence still has PLAYER=nil and all original seven name lookups nil, F4 player identity not verified, and two F9 ABORTs. No game gate PASS. The preceding R030 one-shot R027 census took the *first* 64 matching names from Lua 5.1 `_G` iteration: engine functions matching "player"/"character"/"goh" could fill that arbitrary cap and hide a potentially relevant opaque userdata/table global even if it existed.
R031 modifies only a SEPARATE CLONE-only diagnostic, NOT official R007/v003 or the user's active game. It scans all relevant global names but retains only the top 64 deterministically ordered by Lua TYPE (userdata, table, other nonfunction, function) then lexical name, never reads/logs userdata/table VALUES or pointer addresses. It reports matched and emitted userdata/table counts and total matched functions, so a candidate-alias truncation is explicitly visible rather than silently omitted. It logs PLAYER type even if nil. The optional native camera observer is still disabled by default; no native GOH getter, physics/laser/damage writer, player-handle assignment, gate receipt or game executable is included. An opaque global type/name alone does NOT verify local Alex's handle.
The SHA256-pinned exact original v003 runtime_probe.lua is appended with exact source-only R031 in an independent diagnostic clone that has ONLY the original R007/v003 ASI; this works using the original ASI's F4 Lua reload, not the external executor that would compete for the same native update hook. The actual combined clone-only Lua must have SHA256 d387fceddb45741aef7974c6b66b323f11fec49aff3ebdfb7cb08025edbf4d79; original runtime SHA256 bd7a6ec2006466b0f7b46f966f384103d35d5d98020bcdce1112e9b4f363a650, standalone R031 SHA256 609f2c92ae16f97cffe2b60fd14796a13913c7ec2b375bf70782d306bc52cdb1. Original R007 manager, R007/v003 packages/native and official game remain unchanged. In mocked Lua5.1 CI, two userdata aliases and an opaque table remain visible under 512 competing function names, output is deterministic, and exact original F4 twice still fails closed without PLAYER and never calls unverified native GOH routes. This is not real-game verification. Use only the separately validated R031 offline clone stager/kit, never copy this script to original G:\prototype1\Prototype. An external Gemini/Antigravity run on the user's PC must separately prepare and verify clone, STOP before actual game launch until ready, then collect an authentic clone F4-only log. Do not mark any official gate PASS or install v004+ from this research.