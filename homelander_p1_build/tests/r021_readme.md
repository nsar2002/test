# R021 — Native frame-order and focus-loss source-only replay

The original native C++ BridgeTick dispatches F15 held, F16 dynamic, F19 continuity and F20 read-only DOT callbacks on every foreground game frame if their Lua files loaded, even when an individual mode is disabled. On foreground loss, it first invokes the four corresponding ForceDisable functions. R018 tested each mode separately rather than exercising inactive callbacks between active native callbacks. R021 reuses the exact same assembled 22 Lua native paths and engine mocks, but reruns the ORIGINAL v003 runtime_probe.lua after complete bootstrap as native F4 actually does. It first asserts that real-source F4 fails if the mock PLAYER global is nil, then explicitly supplies a fictitious independently index-verified PLAYER=A as a positive control. This is **not evidence that the user's actual game exposes PLAYER**; their real v001 log said it was nil.

R021 steps through native-order held+inactive-dynamic frames, inactive-held+dynamic frames, continuity+inactive-DOT frames, inactive-continuity+DOT frames, then simulates all four focus-loss ForceDisable callbacks and verifies repeated callbacks cannot emit more laser, damage or VFX. Original R018 R020 suites still run. R021 only strengthens the offline assembled Lua mock; C++ native ABI, scene behavior, player identification, visuals and original R007/v003 live first gate are unproved and unchanged.

The test/README only are packaged; this is not a standalone installer or authorization to change the user's active game. Next authentic game test remains original R007 v003 F4->F9, game exit and evidence ZIP.
