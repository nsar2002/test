# R027 — Single one-shot Lua diagnostic for an ISOLATED Prototype 1 research copy

**DEFAULT IS SAFE/STATIC WITH RESPECT TO GAMEPLAY. THIS IS TEXT SOURCE, NOT AN INSTALLABLE MOD.** The authentic user's active `G:\prototype1\Prototype` has ORIGINAL R007 stage v003 installed, a verified unchanged original loader, authentic F4 failures from `PLAYER=nil` and all seven name routes nil, two F9 aborts, and **NO FIRST-GATE PASS**. R026 found all 14 of the separate Prototype 1 Lua executor's source signatures exactly once in the matching user's original engine DLL, but its native GOMUpdate hook collides with our original Homelander ASI hook; dual-ASI coexistence is unverified. Do not install this diagnostic or an extra ASI into the active original game or use the frozen manager to promote v004+.

`r027_isolated_one_shot_player_diagnostic_SOURCE_ONLY.lua` is a SINGLE complete Lua 5.1 chunk, suitable for later ONE-TIME execution through the independently reviewed third-party Lua executor's editor **ONLY IN A FULLY SEPARATE DISPOSABLE GAME COPY** with one executor ASI and without the Homelander ASI. A clone may still write to common Windows saved-game and settings folders; back those up first. Do not use junctions, symlinks or reparse points pointing at the active user's game root. Preserve the original loader and game engine exact SHA256, verify the launched `prototypef.exe` process path resolves to the disposable copy, and avoid any automatic loop/autorun Lua loader. Gemini can help prepare the copy and verify launch and logs once the user authorizes the actual local experiment. The future executor binary must be sourced and reviewed separately; R027 ZIP contains NO game DLLs, ASIs, injector, installer or executable.

Default `local ENABLE_CAMERA_OBSERVER = false`: the chunk runs ONCE to enumerate only bounded global KEY NAMES and Lua TYPES for identifiers containing player/alex/avatar/character/hero/actor/goh, capped at 64 names and max 72-byte ASCII keys, and logs `PLAYER` type even if nil. It never logs a userdata value, raw pointer, table value, actual character name or memory address. It does not call native game object APIs, change any global, assign `HOMELANDER_PLAYER`, touch an F9 gate, mutate physics/damage/effects, or create a new ASI. The diagnostic only outputs `[P1_R027_ONE_SHOT]` lines; save that text externally as a research artifact. Merely finding a global named `PlayerGOH` with Lua type userdata DOES NOT prove it belongs to controllable Alex.

**SEPARATE EXPLICIT OPT-IN AFTER REVIEW:** Changing the single literal `ENABLE_CAMERA_OBSERVER` to `true` in the standalone script in the disposable research copy would attempt ONE native `ai_GetClosestCharacterToCamera` call and, *only if* a GOH userdata is returned, call read-only go_IsValid, online_DeterminePlayerIndex, go_GetPosition and ai_GetPhysicsVelocity with strict validation. Proximity to the camera alone never establishes local Alex, and even positive `online_DeterminePlayerIndex` may have uncertain single-player semantics. `pcall` cannot catch native crashes or access violations, so this is NOT authorized by an offline mock PASS and MUST NOT be executed without separate explicit user approval. It never sets the Homelander player or any official promotion receipt regardless of result.

**Technical evidence:** GitHub source `uranami/Prototype1-Lua-Tool` commit `c54b297105b54dec992885e81b083c4c0b95f7d3`, 14 unique source signatures in real engine; original active loader `binkw32.dll` SHA256 `bc48bab2cf07eaf616e23ba9eef1922ea5e8590f722b6657a8f2ed986fe6f20e`, engine SHA256 `ed0870eb24ace5565a0245ade42114f0724b881d3d75b793f376e9bb184f8d51`. R027 tests use *only* independent Lua5.1 engine mocks, valid/unstringifiable userdata, fake NPC and broken index cases, no actual game binaries or user evidence logs. This does not test a real executor binary, actual Lua state, native ABI or gameplay. The original R007 v003 gate remains failed. A separately reviewed player-source fix followed by authentic v003 F4+F9 CAMERA/HIT and the ORIGINAL R007 manager Check is required before any promotion.
