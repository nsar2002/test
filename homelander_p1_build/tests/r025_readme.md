# R025 — Exact-game camera-nearest character: read-only *candidate* only

The user's original v003 first-live BOOT/F4/F9 log proves the original `PLAYER` global is nil, the original seven name guesses return nil, and the original F9 therefore aborts. No gate PASS occurred; do not install v004+. The observed game DLL `prototypeenginef.dll` is pinned to SHA256 `ed0870eb24ace5565a0245ade42114f0724b881d3d75b793f376e9bb184f8d51`, length 20,210,960 B, original i386 PE32. Local read-only disassembly at registration VA `0x1037a4cc` identifies the literal Lua API `ai_GetClosestCharacterToCamera` and thunk VA `0x10037ae7` → implementation VA `0x10375d50`; the function traverses game characters, compares a camera distance and writes a selected result to an output object. Its **actual runtime return value/GOH type, stability, local-player identity and ABI safety remain unverified**. The closest character may be an NPC and any ABI mismatch could crash the game. A positive static fingerprint is not proof it is safe to call in a real session.

R025 adds an **OPTIONAL, DORMANT, ONE-SHOT read-only observer**. It checks function presence, count validity, and calls `ai_GetClosestCharacterToCamera` once in a separately authorized real Lua context only after a developer accepts the runtime risk. Only if it returns userdata does it query the already observed `go_IsValid`, `online_DeterminePlayerIndex`, `go_GetPosition` and `ai_GetPhysicsVelocity`, always with pcall and strict nonnegative integer index `< online_GetNumPlayers`. It logs the returned Lua type and whether a candidate passed index/position/velocity checks. It **never logs the GOH object/pointer**, and NEVER sets `HOMELANDER_PLAYER`, installs files, issues a native physics/laser/damage/VFX setter, changes a gate or declares the subject Alex. A Lua pcall cannot protect against native access-violation crashes; only a separately reviewed game-copy test could assess real runtime safety. `go_IsValid` or `online_DeterminePlayerIndex` reporting success does not alone distinguish *local Alex* from a remote player or an NPC misclassification.

CI uses generic synthetic engine mock callbacks (one pass and >10 negative cases including NPC, nil, integer and type errors), actual Lua 5.1 syntax, SHA256-pinned synthetic PE registration/trampoline fingerprint tests. It NEVER uploads user's proprietary engine DLL and never runs real gameplay. `r025_static_closest_camera_abi_audit.py --engine ORIGINAL_PATH` may be run OUTSIDE gameplay to check the exact unchanged file without loading it. This cannot authorize invoking the observer in the active user game. Original frozen R007 manager/manifest/v003 and all source-only R018-R024 and user's game root remain unchanged. First-gate PASS still requires genuine v003 F4 verified handle + F9 CAMERA/HIT and official R007 Check; none of that is established by this research candidate.
