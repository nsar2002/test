V6-M5B0 MaleCNS learner schema protocol

STATUS: frozen before source-value inspection.

This source-only gate qualifies official MaleCNS v1.0 annotation and synapse schemas for future KC/DAN/MBON learner identification. It does not select a final circuit and may not use a model, plasticity outcome, reward, MNQ, PnL, Amin values or M4V0 values.

Frozen discovery rules: report annotation column names and types; enumerate source string values matched only by the literal case-insensitive tokens kenyon, kc, mbon, dan, dopamin, mbin, mushroom; separately enumerate metadata columns whose names contain side, hemi, later or soma; report exact synapse schema and directional body-ID/position fields; use the pinned brain metadata only for ID/schema parity, never to override official annotation taxonomy.

PASS_M5B0_MALECNS_LEARNER_SCHEMA_QUALIFIED requires exact sources to load, a source body identifier, discoverable source taxonomy for KC-like, MBON-like and DAN-like cells, directional synapse IDs/positions, and all firewalls intact. Otherwise BLOCKED_M5B0_MALECNS_LEARNER_SCHEMA.

A PASS authorizes only M5B1 identity-rule freezing from this source report and M4B0 no-market KC-identity parity. Market/reward/PnL remains closed.
